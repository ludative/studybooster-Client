import styled from "styled-components";

export const Input = styled.input`
  border-radius: 3px;
  box-shadow: none;
  border: 1px solid #4a655a;
  min-height: 30px;
  padding: 0 10px;
  font-size: 14px;

  &:focus {
    outline: none;
  }
`;
